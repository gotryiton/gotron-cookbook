description "<%= @service_name %>"

start on filesystem
stop on runlevel [!2345]

respawn
respawn limit 5 30

exec /usr/local/bin/php /var/www/<%= node[:gotron][:app_name] %>/current/app/processes/<%= @process_name %>.php 2>&1 | logger -t '##<%= @service_name.upcase %>##'
